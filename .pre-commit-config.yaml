# Run secret scanning before each commit.
# Setup (once per machine):  brew install pre-commit gitleaks
# Setup (once per clone):    pre-commit install
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
